/*document.addEventListener('DOMContentLoaded', () => {
    const loginButton = document.querySelector('a[href="/login/"]');
    const registroButton = document.querySelector('a[href="/registro/"]');
    const title = document.querySelector('h1');
    const paragraph = document.querySelector('p');

    // Add event listeners for buttons
    loginButton.addEventListener('mouseover', () => {
        loginButton.classList.add('bg-blue-700');
    });

    loginButton.addEventListener('mouseout', () => {
        loginButton.classList.remove('bg-blue-700');
    });

    registroButton.addEventListener('mouseover', () => {
        registroButton.classList.add('bg-blue-300');
    });

    registroButton.addEventListener('mouseout', () => {
        registroButton.classList.remove('bg-blue-300');
    });

    // Add event listener for title click
    title.addEventListener('click', () => {
        alert('Welcome to the BusApp!');
    });

    // Add event listener for paragraph hover
    paragraph.addEventListener('mouseover', () => {
        paragraph.style.color = 'blue';
    });

    paragraph.addEventListener('mouseout', () => {
        paragraph.style.color = 'black';
    });

    // Additional functionalities
    title.addEventListener('dblclick', () => {
        title.style.fontSize = '2em';
    });

    paragraph.addEventListener('click', () => {
        paragraph.style.backgroundColor = 'yellow';
    });
});*/
